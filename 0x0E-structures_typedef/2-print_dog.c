#include "dog.h"
#include <stdio.h>
/**
 * init_dog - intializes a dog
 * @d: the dog to init
 * @name: dog's name
 * @age: dog's age
 * @owner: owner's name
 *
 * Return: void
 */

void print_dog(struct dog *d)
{
	if (d)
	{
		d->name = name;
		d->age = age;
		d->owner = owner;
	}
	if 
	printf("Name: %s\nAge: %lf\nOwner: %s", d->name, d->age, d->owner);
}
